We present a simply deployable interface that allows querying and downloading Holocene sea-level datapoints, formatted following the HOLSEA data template, hosted on a mySQL database. The interface uses AngularJS, is scalable to large datasets and can be deployed in few easy steps, that require only basic knowledge of SQL and HTML.

